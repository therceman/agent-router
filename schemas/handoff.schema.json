{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Agent Router Worker Handoff",
  "type": "object",
  "required": ["schema_version", "task_id", "status", "agent", "files_read", "files_changed", "tests", "manual_checks", "budget", "known_risks", "unresolved_questions"],
  "properties": {
    "schema_version": { "const": 1 },
    "status": { "const": "worker_complete" },
    "tests": { "type": "array", "minItems": 1 }
  },
  "additionalProperties": true
}
